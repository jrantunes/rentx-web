import FormSignIn from "components/FormSignIn";
import Auth from "templates/Auth";

const Login = () => {
  return (
    <Auth title="Estamos quase lá.">
      <FormSignIn />
    </Auth>
  );
};

export default Login;
